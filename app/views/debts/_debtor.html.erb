<% unless @debts.blank? %>
<div class="page-header">
  <h1>Mes dettes</h1>
</div>
<table class="table table-striped">
  <thead>
  <tr>
    <th>Petit surnom</th>
    <th>Texte de rappel</th>
    <th>Valeur</th>
    <th>Date de la créance</th>
    <th>Nom du créancier</th>
    <th><%=t '.actions', :default => t("helpers.actions") %></th>
  </tr>
  </thead>
  <tbody>
  <% @debts.each do |debt| %>
      <tr>
        <td><%= debt.title %></td>
        <td><%= debt.body %></td>
        <td><%= debt.value %>€</td>
        <td><%= debt.date %></td>
        <td><%= debt.creditor.first_name %> <%= debt.debtor.last_name %></td>
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_debt_path(debt), :class => 'btn btn-mini' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      debt_path(debt),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-mini btn-danger' %>
          <%= link_to t('.edit', :default => "Payer" ),
                      solve_debt_path(debt),
                      :method => :put,
                      :class => 'btn btn-mini' %>
        </td>
      </tr>
  <% end %>
  </tbody>
</table>
<% else %>
<div class="well">
  La chance ! Actuellement, vous ne devez de l'argent à personne.
</div>
<% end %>
<h1>Mes dettes passées</h1>
<table class="table table-striped">
  <thead>
  <tr>
    <th>Petit surnom</th>
    <th>Texte de rappel</th>
    <th>Valeur</th>
    <th>Date de la créance</th>
    <th>Nom du créancier</th>
  </tr>
  </thead>
  <tbody>
  <% @old_debts.each do |debt| %>
      <tr>
        <td><%= debt.title %></td>
        <td><%= debt.body %></td>
        <td><%= debt.value %>€</td>
        <td><%= debt.date %></td>
        <td><%= debt.creditor.first_name %> <%= debt.debtor.last_name %></td>
      </tr>
  <% end %>
  </tbody>
</table>